name: Build and Push Docker Image

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            alarastudios/comfyui-chroma-pulid:latest
            alarastudios/comfyui-chroma-pulid:v2
            alarastudios/comfyui-chroma-pulid:v3
            alarastudios/comfyui-chroma-pulid:v4
            alarastudios/comfyui-chroma-pulid:v5
            alarastudios/comfyui-chroma-pulid:v6
          cache-from: type=gha
          cache-to: type=gha,mode=max
